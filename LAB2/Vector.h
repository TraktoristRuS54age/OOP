#pragma once
#include <iostream>
#include <vector>
#include <algorithm>
#include <iomanip>
#include <cmath>
#include <fstream>
#include <unordered_map>

std::vector<float> VectorProcess(std::vector<float>& ArrayNumbers);
bool SortVector(float a, float b);
std::vector<float> VectorPrint(const std::vector<float>& ArrayNumbers);